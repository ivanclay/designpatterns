# Doodling Design Patterns

Design Patterns

Comando para subir o ambiente de desenvolvimento. (Cria e inicia os containers)

​docker-compose up --build -d

Comando para parar e remover os containers.

docker-compose down